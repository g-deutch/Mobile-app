<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Include only if your app benefits from precise location access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymApp"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".activity.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.EditExerciseActivity"
            android:exported="false"
            android:label="Edit Exercise"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.ExerciseActivity"
            android:exported="false"
            android:label="Exercises"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.AllExercisesActivity"
            android:exported="false"
            android:label="All Exercises"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.MyExercisesActivity"
            android:exported="false"
            android:label="My Exercises"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.WorkoutCreatorActivity"
            android:exported="false"
            android:label="Workout Creator"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.ExerciseSelectionActivity"
            android:exported="false"
            android:label="Exercise Selection"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.MainPageActivity"
            android:exported="false"
            android:label="@string/title_activity_Main_Page"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.PremadeActivity"
            android:exported="false"
            android:label ="@string/premade"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.PushActivity"
            android:exported="false"
            android:label ="@string/push"
            android:theme="@style/Theme.GymApp" />

        <activity
            android:name=".activity.SearchActivity"
            android:exported="false"
            android:label="@string/search"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.WorkoutsActivity"
            android:exported="false"
            android:label="@string/workout"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.MyWorkoutsActivity"
            android:exported="false"
            android:label="@string/myWorkouts"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.UpdateAccountActivity"
            android:exported="false"
            android:label="@string/updateAccount"
            android:theme="@style/Theme.GymApp" />
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GymApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>